language: java
jdk: 
 - openjdk7
sudo: true

before_install:
 - sudo dpkg --purge --force-depends ca-certificates-java
 - sudo apt-get install ca-certificates-java

  
script: 
 - cd cn.ieclipse.aorm.core
 - ./gradlew -b build.gradle clean test assemble

